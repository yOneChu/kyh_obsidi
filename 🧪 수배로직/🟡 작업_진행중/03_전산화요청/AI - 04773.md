


1단계
```
# 전산화 요청 '04773' 처리 및 로직 작성 계획

## 1. 개요
전산화 요청 **04773**에 맞춰 PID `CAL_CEILING_PLATE_HOLE_DWG_CMT`에 CEILING PLATE WORKING DWG 관련 주석 출력 로직을 추가하기 위한 프로세스를 진행합니다.

- **전산화 요청번호**: 04773 (담당자: 김영환)
- **대상 PID**: `CAL_CEILING_PLATE_HOLE_DWG_CMT`
- **요청 상세 내용**:
  - `CAL_CEILING_PLATE_HOLE_DWG_CMT`에 아래 내용 추가 요청
  - `CEILING ASSY상 CA, JJ값에 따라 SIDE KICK PLATE 따냄과 T-CABLE BRACKET HOLE의 간섭이 발생하여 해당 작업을 위한 WORKING DWG.입니다.`
  - 문구 내용: `- CEILING PLATE WORKING DWG. 32116116 참고하여 제작할 것.(따냄 및 T-CABLE BRKT HOLE 반영 시)`

---

## 2. 주요 작업 단계

### Phase 1: 로직 작성/변경 템플릿 문서 작성
- `docs/전산화요청_04773.md` 생성
- `logic-write-rule.md` 워크플로우 명세에 맞춰 아래 7가지 항목을 정리하여 기술:
  1. 대상 PID (`CAL_CEILING_PLATE_HOLE_DWG_CMT`)
  2. 변경 유형 및 위치 (기존 `MAIN3_COMMON` 섹션 하단 또는 신규 행 추가)
  3. 적용 조건 (`SPEC` / `CON`) - 특정 조건(CA, JJ 관련 특성)인지, 공통 주석 출력 조건인지 분석 및 명시
  4. 산출 결과 및 출력 내용 (`KEY` / `VAL`)
     - `KEY1`: `L_CMT`, `VAL1`: `- CEILING PLATE WORKING DWG. 32116116 참고하여 제작할 것.(따냄 및 T-CABLE BRKT HOLE 반영 시)`
     - `KEY2`: `CEILING_PLATE_HOLE_DWG_CMT`, `VAL2`: `{CEILING_PLATE_HOLE_DWG_CMT}{L_CMT}\n`
  5. 참고 사항 (수식 및 변수 정의)
  6. 적용 사유
  7. 추가 확인 및 결정이 필요한 정보 (Checklist)

### Phase 2: 사용자 피드백 수렴 및 정합성 검증
- 작성된 템플릿 문서 내용을 기반으로 적용 조건(SPEC/CON) 및 세부 사항 확인
- 기존 `CAL_CEILING_PLATE_HOLE_DWG_CMT` 로직 문법 및 정합성 검증 규칙(`logic-syntax.md`, `logic-validation-rule.md`) 준수 여부 확인

### Phase 3: 로직 작성 결과물 생성
- 최종 확정된 템플릿을 바탕으로 로직 수정을 반영한 CSV/Excel 파일 출력 (`scripts/db_query/query_to_csv.py`, `query_to_excel.py` 활용 또는 결과 출력)

---

## 3. User Review Required

> [!IMPORTANT]
> **적용 조건 (SPEC / CON) 명확화 확인 필요**
> 요청 내용에는 `- CEILING PLATE WORKING DWG. 32116116 참고하여 제작할 것.(따냄 및 T-CABLE BRKT HOLE 반영 시)` 문구 추가만 제시되어 있습니다.
> 이 주석문구가 **무조건(공통) 출력**되어야 하는지, 혹은 **특정 사양/특성 조건(예: 특정 CA, JJ 조건 또는 특정 DWG 적용 시)**에서만 출력되어야 하는지 확인이 필요합니다.

---

## 4. Verification Plan

### Manual Verification
- `docs/전산화요청_04773.md` 템플릿 문서가 지침 양식에 맞게 정상 생성되었는지 확인
- PID `CAL_CEILING_PLATE_HOLE_DWG_CMT`의 기존 구조와 호환되며 문법 오류가 없는지 검증

```

2단계
```
Edited task.md
Viewed %EB%A1%9C%EC%A7%81%EC%9E%91%EC%84%B1%EC%88%98%EC%A0%95%EC%9D%84_%EC%9C%84%ED%95%9C_%ED%85%9C%ED%94%8C%EB%A6%BF_v1.md:1-64
Edited %EC%A0%84%EC%82%B0%ED%99%94%EC%9A%94%EC%B2%AD_04773.md
Edited task.md

전산화 요청 **04773**에 대한 분석 및 로직 작성 템플릿 문서 작성을 완료했습니다.

생성된 문서: [전산화요청_04773.md](file:///c:/anti_workspace/myLogicAgent/docs/%EC%A0%84%EC%82%B0%ED%99%94%EC%9A%94%EC%B2%AD_04773.md)

---

### 주요 정리 내용 (요약)

1. **대상 PID**: `CAL_CEILING_PLATE_HOLE_DWG_CMT`
2. **변경 유형 및 위치**:
   - 신규 안내 주석 출력을 위한 행 추가
   - 위치: `MAIN3_COMMON` 하위 (기존 NO 206 라인 다음 신규 NO 207 라인)
3. **산출 결과 (`KEY` / `VAL`)**:
   - `KEY1`: `L_CMT`
   - `VAL1`: `- CEILING PLATE WORKING DWG. 32116116 참고하여 제작할 것.(따냄 및 T-CABLE BRKT HOLE 반영 시)`
   - `KEY2`: `CEILING_PLATE_HOLE_DWG_CMT`
   - `VAL2`: `{CEILING_PLATE_HOLE_DWG_CMT}{L_CMT}\n`

---

### ⚠️ 추가 확인 및 피드백이 필요한 사항 (Checklist)

1. **적용 조건 (`SPEC` / `CON`)**:
   - 해당 주석 문구가 `CAL_CEILING_PLATE_HOLE_DWG_CMT` 로직 호출 시 **무조건(공통) 출력**되는 것인지, 또는 **특정 사양 조건**(예: 특정 CA, JJ 값 또는 특정 옵션)에만 출력되어야 하는지 확인이 필요합니다.
2. **위치 및 흐름 분기**:
   - `MAIN3_COMMON` 하단에 단순 추가하여 공통 통과시키는 방식이 맞는지 확인 부탁드립니다.

확인해주시면 최종 로직 작성 결과물(CSV/Excel) 출력을 바로 진행하겠습니다.
```