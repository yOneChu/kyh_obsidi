

| 링크
[[PICK_B181A_NEX]]

---

- CWT_LT(CWT높이) 값 `B181AA_ST` 는 `PICK_B181A_NEX`에 정의되어 있다.
- B26_F 처럼 F붙은거는 SUBWEIGHT의 두께이다.
- '[$ ({T_ER} + {B26_F}) * ({T_Q_30} + 1) $]'를 보면 T_ER은 여유값, B26_F는 두께
- `B26_SP`는 CWT의 최대적재높이에 여유공간 `30`을 빼고, `최대적재높이(B26_H)`를 빼서 `0`보다 크면 괜찮은거다.